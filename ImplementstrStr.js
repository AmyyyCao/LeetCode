//Implement strStr

/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
var strStr = function(haystack, needle) {
    //find occurrence of needle in haystack
    //use index of function 
    
    var index = 0;
    index = haystack.indexOf(needle);
    return index;
    
};